language: python

python:
  - 2.6
  - 2.7
  - 3.0
  - 3.5
  - 3.6

services: mysql

env:
  - DJANGO=1.10.5

before_install:
  - export DJANGO_SETTINGS_MODULE=sdnSite.settings
  - export PYTHONPATH=$HOME/builds/Freelance-Connect-dev/SDN.git
  - export PIP_USE_MIRRORS=true

install:
  - pip install -r requirements.txt
  - pip install django==$DJANGO --quiet
  - pip install psycopg2 --quiet

before_script:
  - psql -c "CREATE DATABASE mydb;" -U postgres

script:
  - python manage.py syncdb --noinput